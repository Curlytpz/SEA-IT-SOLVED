import { useEffect, useState } from 'react';
import { ArrowLeft, ArrowRight, X } from '../icons';
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from '../ui/dialog';
import ProtectedCaptureImage from '../hardware/ProtectedCaptureImage';
export default function ImageLightbox({open,onClose,title,subtitle,statusBadge,protectedUrl,imageUrl,alt,positionLabel,onPrevious,onNext}){
  const[actualSize,setActualSize]=useState(false);
  useEffect(()=>{if(!open)return undefined;setActualSize(false);function key(event){if(event.key==='ArrowLeft'&&onPrevious)onPrevious();if(event.key==='ArrowRight'&&onNext)onNext();}window.addEventListener('keydown',key);return()=>window.removeEventListener('keydown',key);},[open,onPrevious,onNext]);
  const imageClass=actualSize?'h-auto w-auto max-w-none object-contain':'max-h-[calc(100dvh-14rem)] w-full object-contain sm:max-h-[calc(100dvh-12rem)]';
  return <Dialog open={open} onOpenChange={next=>{if(!next)onClose();}}><DialogContent showCloseButton={false} className="flex max-h-[calc(100dvh-1rem)] max-w-[min(96vw,1500px)] flex-col gap-0 overflow-hidden p-0 sm:max-h-[calc(100dvh-2rem)] sm:max-w-[min(96vw,1500px)]"><header className="flex shrink-0 flex-wrap items-start justify-between gap-3 border-b border-slate-200 px-3 py-3 dark:border-slate-700 sm:px-5"><DialogHeader className="min-w-0 flex-1"><div className="flex flex-wrap items-center gap-2"><DialogTitle>{title||'Image viewer'}</DialogTitle>{statusBadge}</div>{subtitle&&<DialogDescription>{subtitle}</DialogDescription>}</DialogHeader><div className="flex shrink-0 items-center gap-2"><button type="button" onClick={()=>setActualSize(value=>!value)} className="min-h-11 rounded-lg border border-slate-300 bg-white px-3 text-xs font-semibold text-slate-700 hover:bg-slate-50 dark:border-slate-700 dark:bg-slate-900 dark:text-slate-200">{actualSize?'Fit to screen':'Actual size'}</button><button type="button" onClick={onClose} aria-label="Close image viewer" className="grid h-11 w-11 place-items-center rounded-lg text-slate-500 hover:bg-red-50 hover:text-red-600 dark:text-slate-300 dark:hover:bg-red-400/10"><X size={20}/></button></div></header><div className={`min-h-0 flex-1 bg-slate-950 ${actualSize?'overflow-auto':'overflow-hidden'}`}><div className={`flex min-h-full min-w-full items-center justify-center ${actualSize?'w-max':''}`}>{protectedUrl?<ProtectedCaptureImage url={protectedUrl} alt={alt} className={imageClass}/>:<img src={imageUrl} alt={alt} className={imageClass}/>}</div></div>{(positionLabel||onPrevious||onNext)&&<footer className="flex shrink-0 items-center justify-between gap-3 border-t border-slate-200 px-3 py-3 dark:border-slate-700 sm:px-5"><button type="button" onClick={onPrevious} disabled={!onPrevious} className="inline-flex min-h-11 items-center gap-1.5 rounded-lg px-3 text-xs font-semibold text-slate-600 hover:bg-slate-100 disabled:pointer-events-none disabled:opacity-30 dark:text-slate-300"><ArrowLeft size={16}/> Previous</button><span className="text-center text-xs font-medium text-slate-600 dark:text-slate-300">{positionLabel}</span><button type="button" onClick={onNext} disabled={!onNext} className="inline-flex min-h-11 items-center gap-1.5 rounded-lg px-3 text-xs font-semibold text-slate-600 hover:bg-slate-100 disabled:pointer-events-none disabled:opacity-30 dark:text-slate-300">Next <ArrowRight size={16}/></button></footer>}</DialogContent></Dialog>;
}